import 'package:flutter/material.dart';

class MotorbikePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Xe Máy'),
      ),
      body: Center(
        child: Text('Nội dung trang Xe Máy'),
      ),
    );
  }
}