import 'package:flutter/material.dart';

class BicyclePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Xe Đạp'),
      ),
      body: Center(
        child: Text('Nội dung trang Xe Đạp'),
      ),
    );
  }
}