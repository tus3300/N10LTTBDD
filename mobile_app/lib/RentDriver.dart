import 'package:flutter/material.dart';

class DriverPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Người Lái'),
      ),
      body: Center(
        child: Text('Nội dung trang Người Lái'),
      ),
    );
  }
}